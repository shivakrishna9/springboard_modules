- Pull request: https://github.com/JacksonRiver/springboard_modules/pull/54 by afreeman: Fixed mystery "1" bug.

- Pull request: https://github.com/JacksonRiver/springboard_modules/pull/105 by benclark: Providing an alter hook for changing the $fields array after it has been decrypted in hook_init(). Hook usage:

/**
 * Implements hook_secure_prepopulate_data_alter().
 */
function hook_secure_prepopulate_data_alter(&$data, $nid) {
  if ($node = node_load($nid)) {
    // Add to the prepopulated data.
    $data['node_created'] = $node->created;
  }
  // Remove data, too.
  unset($data['address_line_2']);
}
