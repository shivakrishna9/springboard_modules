<?php

/**
 * @file
 * Simple tests for email_wrappers
 */

/**
 * Sets up basic tools for testing Email Wrappers.
 */
class EmailWrappersTestCase extends DrupalWebTestCase {

  /**
   * Implementation of getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => 'Email Wrappers',
      'description' => 'Sets up basic tools for testing Email Wrappers',
      'group' => 'Email Wrappers',
    );
  }

  /**
   * Implementation of setUp().
   */
  public function setUp($modules = array(), $permissions = array()) {
    $modules = array_merge($modules, array(
      'ctools',
      'webform',
      'email_wrappers',
    ));
    parent::setUp($modules);

    // Create an admin user.
    $permissions = array_merge($permissions, array(
      'access content',
      'administer site configuration',
      'administer content types',
      'administer nodes',
      'administer users',
    ));
    $this->admin_user = $this->drupalCreateUser($permissions);
    $this->drupalLogin($this->admin_user);
  }

  /**
   * Implementation of tearDown().
   */
  public function tearDown() {
    user_cancel(array(), $this->admin_user->uid, 'user_cancel_delete');
    parent::tearDown();
  }

  /**
   * Test installation of dependencies.
   *
   * Remove this test once tests are added for this module.
   */
  public function testInstall() {
    $this->assertTrue(module_exists('email_wrappers'), 'Module was not enabled correctly.');
  }
}
