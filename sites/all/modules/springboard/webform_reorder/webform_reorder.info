; $Id$
name = Webform Creation Re-order
description = Re-orders some elements in the webform creation form so it makes more sense for users.

; Core version (required)
core = 6.x

package = Jackson River SpringBoard
dependencies[] = webform