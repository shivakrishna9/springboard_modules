<?php
/**
 * @file
 * Install, update and uninstall functions for the fundraiser module.
 */

/**
 * Implements hook_uninstall().
 */
function fundraiser_webform_uninstall() {
  db_query("DROP VIEW {fundraiser_webform_data}");
  db_query("DROP VIEW {fundraiser_webform_recurring_order}");
}

/**
 * Implements hook_enable().
 */
function fundraiser_webform_enable() {
  /**
   *  Add the donation_form node type to the array of node types handled
   * by the webform module.
   *
   * The default value is array('webform') because this variable starts
   * off empty, until an admin chooses webform node types at
   * admin/settings/webform. If you save the webform_node_types as an
   * array with ONLY donation_form, then webform nodes will not be
   * handled as webforms.
   */
  $result = variable_get('webform_node_types', array('webform'));
  $result = array_merge($result, fundraiser_get_donation_types());
  variable_set('webform_node_types', $result);

  // New for webform 3.9.
  // 3Let webform know the primary purpose of this node type is for webforms
  $result = variable_get('webform_node_types_primary', array('webform'));
  if (!in_array('donation_form', $result)) {
    $result[] = 'donation_form';
    variable_set('webform_node_types_primary', $result);
  }
}

/**
 * Implements hook_disable().
 */
function fundraiser_webform_disable() {
  // Remove donation_form from the list of webform_node_types.
  // Not really necessary, but still good to clean up after ourselves
  $result = variable_get('webform_node_types', array('webform'));
  if (is_array($result)) {
    foreach ($result as $index => $type) {
      if ($type == 'donation_form') {
        unset($result[$index]);
        variable_set('webform_node_types', $result);
        break;
      }
    }
  }
}

/**
 * Implements hook_schema().
 */
function fundraiser_webform_schema() {
  $schema['fundraiser_webform_order'] = array(
    'description' => 'Tracks the relationship of a webform submission to a specific donation.',
    'fields' => array(
      // Dupe info, belongs elsewhere..
      'webform_nid' => array(
        'description' => 'The original fundraiser node.',
        'type' => 'int',
        'not null' => TRUE,
      ),
      // Dupe info, belongs elsewhere..
      'order_id' => array(
        'description' => 'The donation id.',
        'type' => 'int',
        'not null' => TRUE,
      ),
      'sid' => array(
        'description' => 'The submission id.',
        'type' => 'int',
        'not null' => TRUE,
      ),
      // Salesforce id. This belongs somewhere else.
      'sfid' => array(
        'description' => 'TODO: move this to another module.',
        'type' => 'varchar',
        'length' => '40',
        'not null' => FALSE,
      ),
      // Dupe info, belongs elsewhere..
      'gateway' => array(
        'description' => 'The gateway submitted to.',
        'type' => 'varchar',
        'length' => '100',
        'not null' => FALSE,
      ),
      // Dupe info, belongs elsewhere..
      'txn_id' => array(
        'description' => 'The transaction id of the donation.',
        'type' => 'varchar',
        'length' => '55',
        'not null' => FALSE,
      ),
      // Dupe info, belongs elsewhere..
      'recurring_status' => array(
        'description' => 'The recurring status (y/n) of the donation.',
        'type' => 'int',
        'not null' => FALSE,
        'default' => 0,
      ),
    ),
    'primary key' => array('webform_nid', 'order_id'),
  );
  return $schema;
}

// TODO first update post Port:
// Rename table correctly, rename all order id correctly.
// Resolve the webform order mapping system.
